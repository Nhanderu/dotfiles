#!/usr/bin/env bash

# Installs Arch packages: X11 stuff.
yaourt -S --noconfirm \
    extra/xorg-server \
    extra/xorg-xinit \
    extra/xorg-xrandr

# Installs Arch packages: current window manager.
yaourt -S --noconfirm \
    community/i3-gaps \
    community/i3blocks \
    community/i3lock \
    aur/j4-dmenu-desktop-git \
    aur/i3status-rust

# Installs Arch packages: terminals.
yaourt -S --noconfirm \
    aur/alacritty-scrollback-git \
    aur/alacritty-scrollback-terminfo-git \
    extra/xterm

# Installs Arch packages: programming languages.
yaourt -S --noconfirm \
    community/go \
    community/nodejs \
    community/npm

# Installs Arch packages: editors.
yaourt -S --noconfirm \
    aur/visual-studio-code-bin \
    community/neovim \
    extra/emacs \
    core/vi

# Installs Arch packages: browsers.
yaourt -S --noconfirm \
    extra/firefox \
    aur/tor-browser

# Installs Arch packages: other tools.
yaourt -S --noconfirm \
    community/discord \
    community/rtorrent \
    community/telegram-desktop \
    extra/feh \
    extra/vlc \
    multilib/steam \
    aur/j4-dmenu-desktop-git \
    aur/popcorntime \
    aur/spotify \
    aur/stremio-legacy \
    community/xdotool \
    extra/gimp \
    extra/nitrogen \
    extra/vim \
    extra/zip \
    extra/unrar \
    extra/unzip \
    aur/aura-bin \
    aur/heroku-cli \
    aur/pgcli-git \
    aur/postman

# Install packages that aren't OS-specific.
install-general-pkgs
